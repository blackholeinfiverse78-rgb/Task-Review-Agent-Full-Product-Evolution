services:
  - type: web
    name: parikshak-system
    env: docker
    region: oregon
    healthCheckPath: /health
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        value: '["*"]'
